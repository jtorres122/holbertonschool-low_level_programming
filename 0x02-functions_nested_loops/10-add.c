#include "holberton.h"

/**
 * add - function add two integers and returns the result
 * @free: This was not
 * @bee: a freebee
 * Return: free + bee
 */
int add(int free, int bee)
{
	return (free + bee);
}
